#!/bin/bash
# OpenDomain AI 一键部署脚本示例（MVP版）
# 后续完善：自动拉取仓库、配置环境变量、启动Docker容器
# 使用方式：chmod +x deploy.sh && ./deploy.sh

echo "===== 开始部署 OpenDomain AI ====="
# 预留部署步骤：
# 1. 克隆仓库
# 2. 复制.env.example为.env并配置
# 3. 启动Docker Compose
echo "===== 部署步骤预留，后续完善 ====="
